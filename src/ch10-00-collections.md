# コレクション

<!--
Common collections store multiple values in a single data structure. Lime
provides lists, hash maps, and hash sets through the `collections` package.
-->

一般的なコレクションは、単一のデータ構造に複数の値を格納します。Limeは
`collections`パッケージを通じてリスト、ハッシュマップ、ハッシュセットを提供します。
